# Pixi

## Install Pixi

Go to https://pixi.sh/latest/installation/ and install pixi on your computer.

## Installing Project Dependencies
Once Pixi is installed, navigate to your project directory and run:

```bash
pixi install
```

This will install all dependencies specified in your `pixi.toml` file.